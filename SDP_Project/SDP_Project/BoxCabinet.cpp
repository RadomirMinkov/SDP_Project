#include "BoxCabinet.h"

/*void BoxCabinet::optimizeBoxCabinet()
{
	std::vector<std::string> toDelete;
	for (Box<std::string> box : cabinet)
	{
		unsigned insideBoxesSize = box.getInsideBoxes().size();
		if (box.getSouvenirs().size() == 0 && (!insideBoxesSize || box.getInsideBoxes().size() == 1))
		{
			toDelete.push_back(box.getName());
			
		}
		
	}
}
void BoxCabinet::optimizeBoxCabinetHelper(std::vector<std::string> toDelete)
{

}*/